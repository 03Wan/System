# MySQL SQL Scripts

- `database_init.sql`: Initialize database schema (`thesis_check_system`).
- `database_seed.sql`: Insert demo seed data.

## Suggested order

1. Run `database_init.sql`
2. Run `database_seed.sql`
